on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.1.0
        with:
          server: ftp.guilhermeschubert.com.br
          username: guilh091
          password: 0u5u04WtMk
          server-dir: repo/clonenetflix/
      - name: Copy build
        run: cp -rf /home2/guilh091/repo/clonenetflix/build/* /home2/guilh091/clonenetflix.guilhermeschubert.com.br/